#include <stdio.h>
int main ()
{
    int a,b,c,i,n;
    printf("Enter the number :");
    scanf("%d",&n);
    a=0;
    b=1;
    c=0;
    for(i=1;i<=n;i++)
    {
        printf("%d",a);
        a=b+c;
        b=c;
        c=a;
    }
    return 0;
}
